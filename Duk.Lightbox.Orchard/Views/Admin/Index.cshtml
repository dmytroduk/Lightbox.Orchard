@model Duk.Lightbox.Orchard.Models.ThemeViewModel
@using (Html.BeginForm("Index", "Admin", FormMethod.Post, new { area = "Duk.Lightbox.Orchard", id = "themeForm" }))
{
        @Html.ValidationSummary() 
    <fieldset>
        <label for="currentThemeName">@T("Select Lightbox theme")</label>
        @Html.DropDownListFor(m => m.CurrentThemeName, new SelectList(Model.AvailableThemes, Model.CurrentThemeName))
    </fieldset>
    <fieldset>
        <p>Click on image to preview selected Lightbox theme:</p>
        <p><a id="testImageLink" href="@Model.TestImagePath" target="_blank"><img src="@Model.TestImageThumbnailPath" alt="Click on image to preview selected theme" /></a></p>        
    </fieldset>
    <fieldset>
    @if(Model.IsPreview) 
    {
        <input type="submit" class="button" id="submitButton" value="@T("Save")" />
    }
        @Html.Hidden("isPreview", Model.IsPreview)
        @Html.AntiForgeryTokenOrchard()
    </fieldset>
}

<script type="text/javascript">
    $(function () {
        $("#testImageLink").colorbox();
    });
    $(function () {
        $("#CurrentThemeName").change(function () {
            $("#isPreview").val("true");
            $("#themeForm").submit();
        });
    });
    $(function () {
        $("#submitButton").click(function () {
            $("#isPreview").val("false");
            return true;
        });
    });

</script>